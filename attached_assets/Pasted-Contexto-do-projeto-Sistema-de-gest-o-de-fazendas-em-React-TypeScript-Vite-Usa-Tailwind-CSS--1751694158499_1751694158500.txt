Contexto do projeto:
Sistema de gestão de fazendas em React + TypeScript + Vite.

Usa Tailwind CSS, Radix UI, React Hook Form + Zod, React Query, Wouter.

Backend em Node.js + Express + PostgreSQL usando Drizzle ORM.

Autenticação com passport + express-session, session store com connect-pg-simple.

Na página animals-new, existe um modal de cadastro de animais, atualmente longo e ocupando muito espaço vertical.

✅ Objetivo:
Refatorar o modal de cadastro de animal em animals-new, compactando os campos para reduzir espaço, melhorar usabilidade e tornar o preenchimento mais rápido, mantendo validações e responsividade.

📌 Funcionalidade a ser implementada:
1️⃣ Compactação do formulário:
Organizar os campos relacionados em linhas lado a lado, mantendo legibilidade e responsividade:

Linha 1: Nome (campo único).

Linha 2: Espécie | Raça | Gênero (mesma linha, cada um em um terço da largura).

Linha 3: Data de Nascimento | Peso (kg) (mesma linha, meio a meio).

Linha 4: Estado de Saúde | Mãe | Pai (mesma linha, cada um em um terço).

Linha 5: Observações (campo único).

Responsividade:

Usar grid-cols-1 em telas pequenas e grid-cols-2 ou grid-cols-3 em telas médias/maiores.

Usar Tailwind CSS para gerenciar os grids responsivos.

2️⃣ Tecnologias e práticas:
✅ Frontend:

Usar Radix UI Modal para o modal.

Usar React Hook Form + Zod para validação e controle do formulário.

Usar Tailwind CSS para o layout responsivo.

Utilizar placeholders claros e opcionais em campos não obrigatórios.

Adicionar feedback visual em erros de validação.

Garantir que o modal feche automaticamente ao cadastrar, com toast de sucesso/erro.

Utilizar loading state ao submeter.

✅ Backend:

Manter o endpoint atual de criação de animal utilizando Drizzle ORM.

Garantir que o modal envie os dados no formato esperado pelo backend.

3️⃣ Campos do cadastro:
Nome (text)

Espécie (select)

Raça (text)

Gênero (select)

Data de Nascimento (date)

Peso (kg) (number)

Estado de Saúde (select)

Mãe (text, opcional)

Pai (text, opcional)

Observações (textarea, opcional)

4️⃣ UX desejada:
✅ Modal visualmente limpo, sem scroll excessivo.
✅ Cadastro rápido e intuitivo.
✅ Validações claras, feedback instantâneo em campos inválidos.
✅ Integração fluida com React Query para atualizar a listagem após cadastro.
✅ Coerência com o padrão visual do Lagris.

🚦 Critérios de aceitação:
✅ Modal refatorado sem erros TypeScript.
✅ Campos exibidos corretamente em grid responsivo.
✅ Cadastro funcionando de ponta a ponta.
✅ Feedback visual adequado (loadings, toasts, erros de campo).
✅ Compatibilidade garantida com o fluxo atual de autenticação e fazenda selecionada.